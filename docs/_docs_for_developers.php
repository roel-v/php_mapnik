<?php

namespace Mapnik;

/**
 * Notes for developers of mod_mapnik itself.
 *
 * Building PHP
 *
 * PHP configure command line: --disable-all --enable-cli --enable-debug
 *
 * About unit tests
 *
 * Tried to use PHPUnit. Requires libxml. Need to use debug version to be compatible
 * with rest of build. Hard to build on Windows, ditched the whole idea and used lime.
 *
 * About the macro 'library'
 *
 */
